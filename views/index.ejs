<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Neutral Search</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action=<%= responseId %> method="post">
          <input name="search" type="text" class="ghost-input" placeholder="Enter your search term here" required>
          <input type="submit" class="ghost-button" value="Submit">
        </form>
      </fieldset>

      <% if(searchTerms != null) { %>
        <p class="message">
          Original search term: <b><%= searchTerms[0] %></b><br />
          <% if(searchTerms.length > 1) { %>
            Reverse search term: <% searchTerms.slice(1).forEach((term) => { %>
              <b><%= term %> </b>
            <% }) %>
          <% } %>
        </p>
      <% } %>

      <% if(data != null) { %>
        <% for(var i = 0; i < data.length; i++) { %>
          <div class="result_entry">
            <a href=<%= data[i].link %> target="_blank"><%= data[i].title %></a>
            <p><%= data[i].link %></p>
            <p><%= data[i].snippet %></p>
          </div>
        <% } %>
      <% } %>
    </div>
  </body>
</html>